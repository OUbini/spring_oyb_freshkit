<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.kosmo.oyb.mapper.ProductMapper">

	<select id="getCategoryProductWithPaging" parameterType="map" resultType="edu.kosmo.oyb.vo.ProductVO">
	  <![CDATA[
               SELECT * FROM (
              SELECT ROWNUM AS RNUM, A.* FROM (
                       SELECT * FROM product where category = #{category}   
             ) A WHERE ROWNUM <= #{pageNum} * #{amount}
         ) WHERE RNUM > (#{pageNum}-1) * #{amount}
      ]]>
	</select>

	<select id="getCategoryProductTotalCount" resultType = "int">
	<![CDATA[
		select count(*) from product where category = #{category}
	]]>
	</select>
	
</mapper>
